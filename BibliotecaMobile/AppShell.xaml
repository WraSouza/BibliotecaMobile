<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BibliotecaMobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BibliotecaMobile"
    xmlns:views="clr-namespace:BibliotecaMobile.Views">
    <!--<ShellContent
		Shell.NavBarIsVisible="False"
		Title="Main"
		ContentTemplate="{DataTemplate views:MainPage}"
		Route="MainPage" />-->

    <TabBar>
        <Tab
            Title="Início"
            Icon="home.png"
            Route="MainPage">
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}" />
        </Tab>

        <Tab
            Title="Usuários"
            Icon="group.png"
            Route="UsersView">
            <ShellContent ContentTemplate="{DataTemplate views:UsersView}" />
        </Tab>
    </TabBar>

</Shell>
